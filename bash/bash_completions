# Completions
complete -cf sudo                        # Completion after sudo 
complete -d ls                           # ls completion only directories
complete -W "$(ls ~/virtualenvs)" workon # Completion to custom function

# Git completion
source $HOME/.git-completion.bash
